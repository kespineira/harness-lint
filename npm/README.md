# harness-lint

Local context hygiene and usage analyzer for coding agent harnesses.

The npm package selects a platform-specific native executable through npm's
optional dependencies. The executable is installed locally; this package does
not download or execute network code during installation.
